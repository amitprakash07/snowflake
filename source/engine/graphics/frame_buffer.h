#ifndef ENGINE_GRAPHICS_FRAME_BUFFER_H_
#define ENGINE_GRAPHICS_FRAME_BUFFER_H_

#include "ppm_image.h"
#include "viewport.h"

namespace engine::graphics
{
    class FrameBuffer
    {
    public:
        FrameBuffer() = delete;
        FrameBuffer(const Viewport& viewport)

        {
        }

    private:

    };
}  // namespace engine::graphics

#endif
